import { serviceCar } from '@/config/base-url'
export default {
  // 添加车辆
  ADD_WHITE_CAR: `${serviceCar}/alarm/ignore`,
  // 删除白名单车辆
  DELETE_WHITE_CAR: `${serviceCar}alarm/ignore`,
  // 查询白名单类别
  WHITE_CAR_LIST: `${serviceCar}/alarm/ignore`
}
